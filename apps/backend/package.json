{
	"name": "@workspace/backend",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"build": "bun build ./src/server.ts --target node --format esm --outfile ../../dist/backend/server.js",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"hono": "^4.11.9"
	}
}
